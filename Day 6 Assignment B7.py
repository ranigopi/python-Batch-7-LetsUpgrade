import math
class cone:
	def _init_(self,radius,height):
		self.radius=radius
		self.height=height
		
def volume(self):
	print("enter radius",self.radius)
	print("enter height",self.height)
	print("volume =",math.pi*(self.radius**2)+( self.height/3))
	
def area(self):
	print("enter radius",self.radius)
	print("enter height",self.height)
	print("surface area",math.pi*(self.radius**2))
	print("base",math.pi*self.height*sqrt(self.radius**2+self.height**2))
	
volume(self)
area(self)

class bank():
	def _init_(self,owner,balance):
		self.owner=owner
		self.balance=balance
	def deposist(self):
		if deposist<balance:
			print("withdrawl",self.balance)
	def withdraw(self):
		if withdraw>balance:
			print("over",self.balance)
				
deposist()
withdraw()